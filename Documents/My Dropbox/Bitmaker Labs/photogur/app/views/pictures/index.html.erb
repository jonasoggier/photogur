<h1>Index of all pictures</h1>

<% @pictures.each do |picture| %>
	<h2><%= picture.title %></h2>
	<h3><%= picture.artist %></h3>
	
	<%= link_to picture do %>  <!-- this is the way to do with 	links on images... -->
		<img src="<%= picture.url %>">
	<% end %>

	<%= button_to "Show Details", picture, :method => :get %></p>  
	<!-- alt: link_to "...", picture_path(picture) -->
<% end %>